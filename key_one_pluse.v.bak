module key_one_pulse (
    input  wire clk,
    input  wire key_n,   // active-low
    output wire pulse
);
    reg key_d;

    always @(posedge clk) begin
        key_d <= key_n;
    end

    // Phát xung 1 chu kỳ khi nhấn
    assign pulse = (~key_n) & key_d;

endmodule
